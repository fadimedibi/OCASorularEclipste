package certifica51_100;

public class Q75 {
	/*which three are advantages of the Java exeption mechanism?
	 * 
	 * right answers:
	 * ==>improves the program structure because the error
	 *  code is separated from the normal program function.
	 *  
	 * ==>improves the program structure because the program 
	 * can chose where to handle exception.
	 * 
	 * ==>allows the creation of new exception that are tailored
	 * to the particular program being created.
	 * 
	 * 
	 * wrong answers:
	 * ==>provides a set of standard exceptions that covers
	 *  all(???) possible errors.
	 *  
	 * ==> improves the program structure because exception
	 * must be(???) handled in the method which they accured.
	 *  
		*/
	
}
